@using PortfolioSite.Models

<section id="experience" class="py-24" aria-labelledby="experience-heading">
    <div class="mb-12">
        <h2 id="experience-heading" class="font-display text-4xl font-semibold text-neutral-100 sm:text-5xl">Work history</h2>
        <p class="mt-2 text-neutral-400">The path so far, pinned to the wall.</p>
    </div>

    <div class="space-y-8">
        @foreach (var experience in SiteContent.Experiences)
        {
            <article class="room-panel p-6 sm:p-8">
                <div class="flex flex-col gap-4 sm:flex-row sm:items-start">
                    <div class="sm:w-1/4">
                        <p class="font-mono text-sm text-lamp">@experience.Date</p>
                    </div>
                    <div class="sm:w-3/4">
                        <h3 class="mb-2 font-display text-2xl font-semibold text-neutral-100">
                            @experience.Role — <span class="text-lamp">@experience.Company</span>
                        </h3>
                        <p class="mb-4 leading-relaxed text-neutral-300">@experience.Description</p>
                        <div class="flex flex-wrap gap-2">
                            @foreach (var skill in experience.Skills)
                            {
                                <span class="tech-tag">@skill</span>
                            }
                        </div>
                    </div>
                </div>
            </article>
        }
    </div>
</section>
