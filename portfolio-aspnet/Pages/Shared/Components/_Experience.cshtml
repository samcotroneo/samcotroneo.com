@using PortfolioSite.Models

<div class="border-b border-neutral-700 pb-24">
    <h2 class="my-20 text-center text-4xl">
        <span class="bg-gradient-to-r from-pink-300 via bg-slate-500 to-purple-500 bg-clip-text text-4xl tracking-tight text-transparent">
            Experience
        </span>
    </h2>
    <div>
        @foreach (var experience in SiteContent.Experiences)
        {
            <div class="mb-8 flex flex-wrap lg:justify-center">
                <div class="w-full lg:w-1/4">
                    <p class="mb-2 text-sm text-neutral-400">@experience.Date</p>
                </div>
                <div class="w-full max-w-xl lg:w-3/4">
                    <h6 class="mb-2 font-semibold">
                        @experience.Role - <span class="text-md text-purple-500">@experience.Company</span>
                    </h6>
                    <p class="mb-4 text-neutral-400">@experience.Description</p>
                    <div class="flex flex-wrap">
                        @foreach (var skill in experience.Skills)
                        {
                            <span class="whitespace-nowrap rounded bg-neutral-900 text-purple-500 text-sm font-medium mr-2 mt-2 px-2 py-1">
                                @skill
                            </span>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>
